PORT=3000
# Listen on all network interfaces so classmates on the same network can connect.
HOST=0.0.0.0
# Database settings are only needed for /api/health, not the mock transcript API.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=csc425fa26
DB_USER=postgres
DB_PASSWORD=replace_with_your_password
